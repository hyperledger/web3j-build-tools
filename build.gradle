plugins {
    id 'com.diffplug.gradle.spotless' version '3.23.1'
    id 'jacoco'
}

apply {
    ['jacoco', 'java', 'javadoc', 'repositories', 'spotless', 'publish'].each {
        from("$rootDir/gradle/$it/build.gradle")
    }
}